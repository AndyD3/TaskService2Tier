export type Task = {
  id: number
  title: string
  description: string
  status: string
  dueDate: string
}

export type TaskInput = {
  title: string
  description: string
  status: string
  dueDate: string
}
